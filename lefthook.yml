# Refer for explanation to following link:
# https://lefthook.dev/configuration/

pre-commit:
  parallel: true
  jobs:
    - run: bun check {staged_files}
      glob: "*.{ts,tsx}"

pre-push:
  parallel: true
  jobs:
    - run: bun type-check
      glob: "*.{ts,tsx}"
